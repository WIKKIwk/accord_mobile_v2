// Generated by large_library_splitter.dart; keep changes in the source library.
part of 'admin_telegram_screen.dart';

class _TelegramUserCard extends StatelessWidget {
  const _TelegramUserCard({required this.user});

  final TelegramUserAccount user;

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    final scheme = theme.colorScheme;
    final isAdmin = user.role == TelegramInviteRole.admin;
    final username = user.username.trim();
    return M3SegmentFilledSurface(
      slot: M3SegmentVerticalSlot.bottom,
      cornerRadius: M3SegmentedListGeometry.cornerLarge,
      backgroundColor: scheme.surfaceContainerLowest,
      child: Padding(
        padding: const EdgeInsets.fromLTRB(16, 14, 16, 14),
        child: Row(
          children: [
            CircleAvatar(
              backgroundColor: scheme.secondaryContainer,
              foregroundColor: scheme.onSecondaryContainer,
              child: Icon(
                isAdmin
                    ? Icons.admin_panel_settings_outlined
                    : Icons.point_of_sale_outlined,
                size: 20,
              ),
            ),
            const SizedBox(width: 12),
            Expanded(
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Text(
                    user.displayName.trim().isEmpty
                        ? context.l10n.adminTelegramUnknownUser
                        : user.displayName,
                    style: theme.textTheme.titleMedium?.copyWith(
                      fontWeight: FontWeight.w700,
                    ),
                  ),
                  const SizedBox(height: 3),
                  Text(
                    '${username.isEmpty ? '' : '@$username  '}ID: ${user.telegramUserId}',
                    style: theme.textTheme.bodySmall?.copyWith(
                      color: scheme.onSurfaceVariant,
                    ),
                  ),
                  const SizedBox(height: 7),
                  Wrap(
                    spacing: 6,
                    runSpacing: 4,
                    children: [
                      _TelegramStatusPill(
                        label: user.deliveryMode == TelegramDeliveryMode.bot
                            ? context.l10n.adminTelegramDeliveryBot
                            : context.l10n.adminTelegramDeliveryUserProfile,
                        color: scheme.primaryContainer,
                        foregroundColor: scheme.onPrimaryContainer,
                      ),
                      if (user.deliveryMode == TelegramDeliveryMode.userProfile)
                        _TelegramStatusPill(
                          label: user.userProfileConnected
                              ? context.l10n.adminTelegramUserProfileConnected
                              : context
                                  .l10n.adminTelegramUserProfileNotConnected,
                          color: user.userProfileConnected
                              ? scheme.tertiaryContainer
                              : scheme.errorContainer,
                          foregroundColor: user.userProfileConnected
                              ? scheme.onTertiaryContainer
                              : scheme.onErrorContainer,
                        ),
                    ],
                  ),
                  if (user.selectedChatTitle?.trim().isNotEmpty == true) ...[
                    const SizedBox(height: 4),
                    Text(
                      '${context.l10n.adminTelegramSelectedGroup}: ${user.selectedChatTitle}',
                      maxLines: 1,
                      overflow: TextOverflow.ellipsis,
                      style: theme.textTheme.bodySmall?.copyWith(
                        color: scheme.onSurfaceVariant,
                      ),
                    ),
                  ],
                ],
              ),
            ),
            Text(
              user.role.label(
                adminLabel: context.l10n.adminTelegramAdminRoleTitle,
                salesManagerLabel:
                    context.l10n.adminTelegramSalesManagerRoleTitle,
              ),
              style: theme.textTheme.labelMedium?.copyWith(
                color: scheme.primary,
                fontWeight: FontWeight.w700,
              ),
            ),
          ],
        ),
      ),
    );
  }
}

class _TelegramStatusPill extends StatelessWidget {
  const _TelegramStatusPill({
    required this.label,
    required this.color,
    required this.foregroundColor,
  });

  final String label;
  final Color color;
  final Color foregroundColor;

  @override
  Widget build(BuildContext context) {
    return DecoratedBox(
      decoration: BoxDecoration(
        color: color,
        borderRadius: BorderRadius.circular(999),
      ),
      child: Padding(
        padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 3),
        child: Text(
          label,
          style: Theme.of(context).textTheme.labelSmall?.copyWith(
                color: foregroundColor,
                fontWeight: FontWeight.w700,
              ),
        ),
      ),
    );
  }
}

class _TelegramUserGroup extends StatelessWidget {
  const _TelegramUserGroup({
    required this.title,
    required this.users,
  });

  final String title;
  final List<TelegramUserAccount> users;

  @override
  Widget build(BuildContext context) {
    if (users.isEmpty) {
      return const SizedBox.shrink();
    }
    return Padding(
      padding: const EdgeInsets.only(bottom: 12),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Padding(
            padding: const EdgeInsets.only(left: 4, bottom: 6),
            child: Text(
              title,
              style: Theme.of(context).textTheme.titleSmall?.copyWith(
                    fontWeight: FontWeight.w800,
                  ),
            ),
          ),
          for (var index = 0; index < users.length; index++)
            Padding(
              padding: EdgeInsets.only(
                bottom: index == users.length - 1 ? 0 : 4,
              ),
              child: _TelegramUserCard(user: users[index]),
            ),
        ],
      ),
    );
  }
}

class _TelegramEmptyUsersCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final scheme = Theme.of(context).colorScheme;
    return M3SegmentFilledSurface(
      slot: M3SegmentVerticalSlot.top,
      cornerRadius: M3SegmentedListGeometry.cornerLarge,
      backgroundColor: scheme.surfaceContainerLowest,
      child: Padding(
        padding: const EdgeInsets.all(18),
        child: Text(
          context.l10n.adminTelegramNoUsers,
          style: Theme.of(context).textTheme.bodyMedium?.copyWith(
                color: scheme.onSurfaceVariant,
              ),
        ),
      ),
    );
  }
}
