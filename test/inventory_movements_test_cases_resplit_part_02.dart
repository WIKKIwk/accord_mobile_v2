// Generated by large_library_splitter.dart; keep changes in the source library.
part of 'inventory_movements_test.dart';

void _registerinventory_movements_testCases02({
  required InventoryLocation source,
  required InventoryLocation destination,
  required InventoryLocation factoryState,
  required InventoryAsset asset,
  required InventoryAsset otherWarehouseAsset,
  required InventoryTransfer transfer,
}) {
  testWidgets('inventory FAB opens QR lookup and lists scanned assets', (
    tester,
  ) async {
    AppSession.instance.profile = const SessionProfile(
      role: UserRole.materialTaminotchi,
      displayName: 'Materialchi',
      legalName: '',
      ref: 'material-1',
      phone: '',
      avatarUrl: '',
      capabilities: ['inventory.movement.manage'],
      assignedWarehouses: ['Material ombor', 'Qolip ombor'],
    );
    seedMobileApiInventoryMovementTestData(
      locations: [source, destination, factoryState],
      assets: [asset, otherWarehouseAsset],
      transfers: [transfer],
    );

    await tester.pumpWidget(
      const MaterialApp(
        locale: Locale('uz'),
        localizationsDelegates: [
          AppLocalizations.delegate,
          GlobalMaterialLocalizations.delegate,
          GlobalCupertinoLocalizations.delegate,
          GlobalWidgetsLocalizations.delegate,
        ],
        supportedLocales: AppLocalizations.supportedLocales,
        home: InventoryMovementsScreen(),
      ),
    );
    await tester.pumpAndSettle();

    await tester.tap(find.bySemanticsLabel('Amallar'));
    await tester.pumpAndSettle();
    await tester.tap(find.text('QR orqali izlash'));
    await tester.pumpAndSettle();

    expect(
      find.byKey(const ValueKey('inventory-qr-lookup-panel')),
      findsOneWidget,
    );
    final scanner = tester.widget<ProductionQuickScannerPanel>(
      find.byKey(const ValueKey('inventory-qr-search-scanner')),
    );
    await scanner.onCodeDetected('30CC');
    await tester.pumpAndSettle();

    expect(
      find.byKey(const ValueKey('inventory-qr-search-result-raw:b')),
      findsOneWidget,
    );
    expect(find.text('Polipropilen'), findsOneWidget);
  });

  testWidgets('warehouse filter is restored when movement screen is reopened', (
    tester,
  ) async {
    AppSession.instance.profile = const SessionProfile(
      role: UserRole.materialTaminotchi,
      displayName: 'Materialchi',
      legalName: '',
      ref: 'material-1',
      phone: '',
      avatarUrl: '',
      capabilities: ['inventory.movement.manage'],
      assignedWarehouses: ['Material ombor', 'Qolip ombor'],
    );
    seedMobileApiInventoryMovementTestData(
      locations: [source, destination, factoryState],
      assets: [asset, otherWarehouseAsset],
      transfers: [transfer],
    );

    Future<void> openScreen() async {
      await tester.pumpWidget(
        const MaterialApp(
          locale: Locale('uz'),
          localizationsDelegates: [
            AppLocalizations.delegate,
            GlobalMaterialLocalizations.delegate,
            GlobalCupertinoLocalizations.delegate,
            GlobalWidgetsLocalizations.delegate,
          ],
          supportedLocales: AppLocalizations.supportedLocales,
          home: InventoryMovementsScreen(),
        ),
      );
      await tester.pumpAndSettle();
    }

    await openScreen();
    await tester.tap(
      find.byKey(const ValueKey('inventory-warehouse-filter-chip')),
    );
    await tester.pumpAndSettle();
    await tester.tap(
      find.byKey(const ValueKey('inventory-warehouse-option-warehouse:b')),
    );
    await tester.pumpAndSettle();
    expect(find.text('Ombor: Qolip ombor'), findsOneWidget);

    await tester.pumpWidget(const SizedBox.shrink());
    await tester.pumpAndSettle();
    await openScreen();

    expect(find.text('Ombor: Qolip ombor'), findsOneWidget);
    expect(find.text('Polipropilen'), findsOneWidget);
    expect(find.text('Polietilen'), findsNothing);
  });

  testWidgets('warehouse filter is available on all movement tabs', (
    tester,
  ) async {
    AppSession.instance.profile = const SessionProfile(
      role: UserRole.materialTaminotchi,
      displayName: 'Materialchi',
      legalName: '',
      ref: 'material-1',
      phone: '',
      avatarUrl: '',
      capabilities: ['inventory.movement.manage'],
      assignedWarehouses: ['Material ombor', 'Qolip ombor'],
    );

    await tester.pumpWidget(
      const MaterialApp(
        locale: Locale('uz'),
        localizationsDelegates: [
          AppLocalizations.delegate,
          GlobalMaterialLocalizations.delegate,
          GlobalCupertinoLocalizations.delegate,
          GlobalWidgetsLocalizations.delegate,
        ],
        supportedLocales: AppLocalizations.supportedLocales,
        home: InventoryMovementsScreen(),
      ),
    );
    await tester.pumpAndSettle();

    final filterKey = const ValueKey('inventory-warehouse-filter-chip');
    expect(find.byKey(filterKey), findsOneWidget);

    await tester.tap(find.text('Kiruvchi'));
    await tester.pumpAndSettle();
    expect(find.byKey(filterKey), findsOneWidget);

    await tester.tap(find.text('Chiquvchi'));
    await tester.pumpAndSettle();
    expect(find.byKey(filterKey), findsOneWidget);

    await tester.tap(find.byKey(filterKey).first);
    await tester.pumpAndSettle();
    expect(find.text('Qolip ombor'), findsWidgets);

    await tester.tap(
      find.byKey(const ValueKey('inventory-warehouse-option-warehouse:b')),
    );
    await tester.pumpAndSettle();
    expect(find.text('Ombor: Qolip ombor'), findsOneWidget);
  });
}
